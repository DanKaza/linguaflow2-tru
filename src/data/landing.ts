/**
 * Landing page content — single source of truth.
 *
 * Customizing LinguaFlow for another school = editing THIS file
 * (school name, stats, contact) + seeding the database.
 */

/** Identitas sekolah utama platform ini. */
export const schoolName = "SMK Texar";

/** Statistik — jumlah murid yang sudah pernah masuk & belajar. */
export const stats = {
  /** Total murid yang pernah login (sementara statis, nanti bisa diambil dari database). */
  totalMurid: 47,
  /** Kosakata yang dikuasai. */
  kosakata: "800+",
  /** Jam belajar terkumpul. */
  jamBelajar: "1.200+",
};
